export default class Seller{

    constructor(id, name, address, email, identifier) {
        this.id = id
        this.name = name
        this.address = address
        this.email = email
        this.identifier = identifier
    }
}